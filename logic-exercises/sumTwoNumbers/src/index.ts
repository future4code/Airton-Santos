export function sumTwoNumbers(nums: number[], target: number) {
    let indexOfNums: number[] = [];

    for (let i = 0,  !== 0, ) {
        indexOfNums.push(i)
    }
    return 
}

let numbers: number[] = [44, 55, 66, 77]

console.log(sumTwoNumbers(numbers, 100))

console.log(numbers.indexOf(66))